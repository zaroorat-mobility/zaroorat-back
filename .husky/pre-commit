#!/usr/bin/env sh
. "$(dirname "$0")/common.sh"

printf "\033[36m🔍 Running pre-commit checks...\033[0m\n"

# Staged files only — keep this fast. Full typecheck runs on pre-push.
npx --no-install lint-staged

# Secret Scan (later)
# TODO: Add gitleaks to detect AWS Keys, JWT Secrets, API Keys before commit
# gitleaks protect -v --staged

printf "\033[32m✅ Pre-commit checks passed.\033[0m\n"
