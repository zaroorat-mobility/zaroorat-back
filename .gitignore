# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# Generated Prisma client. Regenerated by `npm run prisma:generate`; it is
# ~22MB of machine-written code (including WASM engines) and must never be
# reviewed, linted, or committed.
src/generated/

# Demo Frontend

ride-demo-frontend/

# Environment files
.env
.env.*
!.env.example
# .env.test holds only throwaway values pointing at local/CI service containers.
# Committing it means `npm test` works on a fresh clone and in CI with no setup.
# Never put a real credential in it.
!.env.test

# Logs
*.log

# Coverage
coverage/

# IDE / OS
.vscode/
.idea/
.DS_Store
