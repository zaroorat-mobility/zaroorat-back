apiVersion: v1
kind: Service
metadata:
  name: {{ include "zaroorat-backend.fullname" . }}
  labels:
    {{- include "zaroorat-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zaroorat-backend.selectorLabels" . | nindent 4 }}
