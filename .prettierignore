# Build output and dependencies
node_modules/
dist/
coverage/
*.tsbuildinfo

# Lockfile — npm owns its formatting.
package-lock.json

# Helm templates are Go templates, not YAML. Prettier's YAML parser rejects
# `{{- if ... }}` at the top level, so it cannot format these files at all.
# Chart.yaml and the values-*.yaml files are plain YAML and stay formatted.
infrastructure/helm/templates/

# Separate browser app with its own toolchain. Git already ignores it, but
# Prettier does not read .gitignore, so it has to be listed here too.
frontend/

# Generated
prisma/migrations/
src/generated/
